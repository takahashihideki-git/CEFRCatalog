# CLAUDE.md

CEFRカタログ（株式会社トピックメーカー）。プロジェクトの中身は `README.md` に、設計と作業仕様は `deliverables/` の2文書にある。ここには**作業を始める前に知っておくべき規律**だけを書く。

## 作業前に必ず読む（順序が重要）

1. `deliverables/CEFR全貌カタログ_企画書.md` ── グランドデザイン（何を作るか）
2. `deliverables/CEFRカタログ_引き継ぎ書.md` ── 作業仕様（どう作り続けるか）。§4に周回計画と現在地

**引き継ぎ書だけで作業を始めないこと。** 企画書未読のまま、確定済みの設計判断へ揺り戻しが起きかけた実績がある（引き継ぎ書 設計判断(j)）。

## 変更したら必ず検証を通す

```bash
python3 restore.py            # 1224件／篩266・ADOPT183／行為22／全数シート29枚〔第1柱22＋第2柱7〕／第2柱インベントリ132／並行対3族12／糸正準7／テンプレート三層／参照台帳7種46／軸台帳13軸182／表示訳4門（要約。内訳はrestore.pyの出力が正）
python3 analysis/partition.py # block_partition_1224.json を再生成し7区分の件数を検証（篩採用183/除外83/対象外40）
python3 tools/source_sync_check.py  # 生成ソース↔成果物の同期（restore.pyの範囲外を埋める。下記参照）
```

新しい範型（`prototypes/*_prototype.py`）を追加・変更したときは、機械検証も併せて行う。周をまたぐ不変条件は引き継ぎ書§4にある通り：

- **No. が正準ID**。原文（`en`）とレベルは `data/descriptors_en_1224.json` と一致すること
- **範型は10列＋DISCUSSION 5段落**。`.py` と統合JSON（`prototypes_4types.json`＝4範型／各 `verification_*.json`＝検証範型①〜⑤）の内容が完全一致すること。統合JSONは行為名をトップキーにした dict、行dictの列キーは `mode,level,no,en,jp,exponents,scene,howwell,l1,delta`（py注記の `l1_note` ではなく **`l1`** が正準）。`restore.py` は統合JSONの行を原典と照合するだけで `.py` を見ない ── **`.py`↔統合JSONの完全一致は `tools/source_sync_check.py` が担う**（旧範型9枚の全10列を照合。統合JSON側に対応`.py`の無い行為が残っていないかも見る）
- **第3周の全数シート（`catalog_*.json`、第1カタログ全22行為ぶんが揃済み。`restore.py` の `CATALOGS` 登録表が正）は生成物**。ソースは `prototypes/<act>_full_prototype.py`（en/jp は data から構築時取得＝単一情報源）。`restore.py` が原文・レベル・訳・行為所属に加え**全数性**（シートの行集合＝インベントリの当該行為の所属集合）を照合する。ソースと成果物の突き合わせは **`tools/source_sync_check.py`**（各 `build()` の結果をメモリ上で JSON と比較するので**コミット前でも使える** ── 従来の「適用→コミット→再生成→`git diff` が空」の順は不要になった。git方式はコミット前に `--exit-code` を使うとパッチ自身の差分で必ず失敗する罠があった）。あわせて `tools/exponent_level_check.py` を通す（第3周-0の品質ゲート）
- **正準ファイル名は数字入りで動く**。採用数・篩件数の確定でリネームされる ── 現行は `data/inventory_183to22.json`・`data/sieve_verdicts_266.json`（第3周-5・判断(v)でNo.612採用）・`data/p2_inventory_132to7.json`（第2柱、判断(ah)。母数132は区分分割で固定＝改名は想定しない）。改名時は `restore.py` と `analysis/partition.py` の**両方**の参照名を追随させる（過去にpartition.py側が漏れた実績あり。EXPECTED_SUB等の数字も同時に見る）。`tools/source_sync_check.py` は glob で拾うので改名に自動追随する

- **成果物JSONだけ直して生成ソースを取り残さない**。同じ数字・同じ名前が `.py` 側にもある ── 実際に3回起きた（`partition.py` の参照名／同 `EXPECTED_SUB`／`verdicts.py` にNo.612未追記）。パッチが `data/*.json` を触ったら、対応する `analysis/*.py`・`prototypes/*.py` が追随しているかを `tools/source_sync_check.py` で確かめる

**作業訳を直すときは二重管理に注意**。作業訳は生成ソース `analysis/ja_tr1〜9.py`（`JA1〜JA9` dict）と成果物 `data/working_translations_1224.json` の両方にある。片方だけ直すと生成経路がずれる。両方を同期修正し、`ja_tr*.py` 合成 == JSON 全1,224件一致を確認してから `restore.py` を通す（この一致確認も `tools/source_sync_check.py` が行う）。同じ二重管理が篩帳簿にもある ── `analysis/verdicts.py`（`V` dict）と `data/sieve_verdicts_*.json`。

解像度を上げるのは中身であって器ではない。器（スキーマ）を変えるときは引き継ぎ書に判断を書き足す。

**参照台帳（`data/crossrefs.json`）にエッジを足すときは散文証拠が先**。restore.pyの散文同期ゲート（判断(ai)③＝台帳⊆散文）は、片側の行散文・DISCUSSION・p2_inventoryのnoteに相手の記録がないエッジを落とす ── エッジ追加は読み（なぜ対か）のdelta追記とセットで行う。逆方向のassert（散文⊆台帳）は意図的に無い。善意で足さないこと（正規表現寄生の脆さを持ち込む ── 散文側の網羅はgrep一掃の人間規律）。既存帳簿（mode_pairs・act_phases・cross_axes）が所有する参照種はcrossrefsに載せない（所有排他①）。

**判断を1つ追記したら、二次コピー発生箇所を定型パトロールする（散文追随チェックリスト）**。機械ゲートは正準↔正準しか見ない ── 判断が増えるたびに、その事実の二次コピーが散文側に3〜6か所生まれ、パッチが新しい正準と直近の散文だけを触ると古い場所のコピーが取り残される（実例：カタログ12検品で6件＋パトロール1件＝計7件の残置を一括補修）。パッチを閉じる前に次を必ず見る：①README「いまどこにいるか」 ②READMEディレクトリ構成 ③README復元検証値（新assertの行追加と、既存行の数値・文言） ④引き継ぎ書§1 ⑤引き継ぎ書§2（当該判断の本文） ⑥引き継ぎ書§7（同じ事項の申し送りが残っていれば取り消し線＋完了へ） ⑦企画書（構造の数値・見出しに触れた場合） ⑧CLAUDE.md自身。数値（件数・枚数・対数）を変えた判断は、その数値の**全文書grep**を追随の一部とする。**ツールの照合対象を説明した散文もこの類**（README「開発体制」の `source_sync_check.py` の説明＝実例、判断ではなくゲート拡張で古くなる）。ただし**`deliverables/CEFRカタログ_書物ロードマップ.md` は巡回対象外**（完成規模の推計文書。567ページ・53章・658段などの見積もり値を持つが正準ではなく、全文書grepで拾っても追随させない ── 古くなったら誤りではなく測り直す時期が来たというだけ。文書冒頭に同じ断りがある）。

**語彙・名称を改名/追加したら散文を grep で一掃する**。機械ゲート（`restore.py`・`source_sync_check.py`）は構造・データ・`.py`↔JSON一致は照合するが、**散文（`type`/`delta`/DISCUSSION/`note`/READMEの説明文）の用語の一貫性は見ない** ── 全緑のまま旧語が残る。実際に族再編（判断ad「系→族」）で退役語「論証系」の生きた用法が5箇所残り追補パッチになった（原因＝正準データの grep だけで確認し、同スレッドで新規に書いた散文を対象に含めなかった）。生成物の再生成後に全ファイル走査を最終ゲートに入れる。例（族再編時）：`grep -rn "叙述系\|論証系\|教示系\|共通糸" --include=*.py --include=*.json --include=*.md . | grep -v "下位系\|感情叙述系\|当時\|deliverables/"`。**保全して直さない旧語**：①引用（「当時『共通糸』」等）②改名記述そのもの（p2_threads「判断(ad)の経緯」＝旧称を名指しして改名を説明）③各スレッド時点の履歴記述（README/引き継ぎ書の過去カタログ段落 ── 判断は根拠＝履歴ごと残す）④別概念の同綴り（第1柱の下位系名 `cross_axes` の `下位系:叙述系`・`act_phases` の `感情叙述系`）。

## 原典ファイルはコミットしない

CoE配布の `CEFR Descriptors (2020).xlsx` とゲーテ版PDFは `.gitignore` で除外済み。**通常の作業では原典xlsxは不要**（原文テキストは `data/descriptors_en_1224.json` に収録済み、CoEが出典明記のもとで再利用を許諾）。ファイル名のゆらぎに注意 — 除外パターンは `*Descriptors*.xlsx`。

## 成果物の受け取りと統合（このリポジトリでの主な役割）

カタログ本体（範型・データ・ドキュメント）の執筆は**別スレッド（Windows側）**で行われ、ここでの役割は**検証・整合確認・コミット**。成果物は通常、`files.zip` に入った パッチ（`*.patch`／`*.diff`）＋`申し送り_ClaudeCode*.md` で届く。手順：

1. `bash tools/clean_zone_identifier.sh` で `*:Zone.Identifier` を掃除
2. `申し送り_ClaudeCode.md` を読み、`git apply --stat` / `--check` と **diff本文の目視**で内容を申し送りと突き合わせる
3. `git apply roundN_M.diff` → `python3 restore.py` と `python3 analysis/partition.py`（＋範型を触ったら `.py`↔統合JSON照合）を通す
4. 申し送りの表題でコミット → push → 作業用ファイル（`*.diff`・`*.patch`・`申し送り*.md`・`files.zip`・**同梱の単体データファイル**）を削除

**検品指摘は次スレッドに送らず、同じカタログ内の「補遺」パッチで閉じる**。執筆スレッドは前スレッドの修正を持ち越さず新しい目標へ向かう方針なので、指摘を「カタログN+1への申し送り」に積むと宙に浮く ── 出した指摘はその場で全部出し切り、補遺パッチを受けて再検証する往復を前提にセッションを組む（カタログ16は本体＋補遺3本＝1セッションで4パッチ、`ad48acd`〜`b6e949e` で裁定待ちゼロまで閉じた）。**書き換えの向きが執筆判断になる指摘も、こちらで直さずに補遺へ回してよい**（何を直すかは出す、どう直すかは向こうが決める）。

**新設データの単体コピーが同梱されることがある**（例：`p2_inventory_132to7.json`・`crossrefs.json` がパッチとは別に zip 直下に入る）。パッチ側にも同じファイルが含まれているので、**両者の内容一致を確認**してから単体は削除する（版ずれ＝コピー漏れの検出点。zip直下の `*.json` は `.gitignore` の対象外なので、消さずに `git add -A` すると混入する）。

**新しい assert を受け取ったら反例で落として確かめる**。全緑は「規則が守られている」証拠であって「規則が検査されている」証拠ではない ── 空振りの assert は全緑のまま通る。正準JSONを一時退避してわざと壊し（件数を1件減らす／`scale`・`level` を改竄する／帳簿間で行を移す／必須noteを空にする）、`restore.py` が**想定どおりのメッセージで**落ちることを確認してから元に戻す。判断(ag)の固有糸3規則・判断(ah)の帳簿5点はこの手順で検証済み。**反例は必ず正準データから生成する** ── 手で書いた反例が発火しないとき、原因はゲートの欠陥と反例の失敗のどちらでもありえて、見分けがつかない。正準に違反の実例がない語形へゲートを先回りで広げる場合（双子パターンの対称化等）は、**正準文からの最小変異**（正準の違反文面の参照語1語だけを差し替える等）を反例とする ── jp一字改変と同族の生成法であり、発火しないときも原因の候補が差し替えた一語に閉じる（カタログ18補遺2の裁定）（カタログ16補遺の破壊テスト12件中1件が該当。素材No.622は本文に「（No.324, 622）」という群引用で出るため、リテラル `No.622` の除去では本文が1文字も変わっておらず、緑が正しかった）。**カタログ16で実際に空振りが1件出ている** ── `book_style_check.py` の章側「No.が本文に出現しない」検査が出典行を含む全文を見ており、出典行は同関数内で行集合との一致をassertされているため構造上発火不能だった（幕間側は正しく `出典：` で切っていた）。

**受け取りのたびに散文の二次コピーを巡回する**（執筆側の散文追随チェックリストと両側から挟む体制。カタログ12で計10件の残置を摘出した）。見るのは**現在状態を語る5か所** ── README ディレクトリ構成／README 復元検証値／README 開発体制（ツールの照合対象の説明）／引き継ぎ書§7／企画書。**履歴記述（README「いまどこ」・引き継ぎ書§1・§2の判断本文）は保全**＝当時の数字のままが正。ただし**保全されるのは過去の周の記述だけで、いま受け取っている周が自分について書いた段落は保全対象でない** ── そこは「当時＝いま」なので、事実と違えばただの誤りである（カタログ16では、README「いまどこ」の当該周の段落に「議論3節」〔実データは2節〕と「精読裁定待ち」〔同じパッチの§2・§7は「承認済み」〕が入っていた）。**同じ事項の状態語が文書をまたいで割れていたら追随漏れ**（同一文書内の新旧同居の文書横断版）。状態語（裁定待ち／承認済み／完了／議論N節／KNOWN_GAPS 等）での横断grepは執筆側の規律にもなった（引き継ぎ書§4「申し送り前の点検規律」）が、こちらでも一度回す。コツは二つ：**①同じ文書に新旧の数字が同居していたら追随漏れの決定的な徴候**（README が「論証族1対」と「件数〔7・4・1〕」を併記、企画書が「篩265件」と「266件」を併記＝いずれも当たり）**②スイープは全件表示して件別に分類する** ── 先頭数件で打ち切って総括すると生きた古参照を見落とす（同じ文が別ファイルにもある「双子」を両側とも一度取りこぼした）。正準側の件数表はスクラッチパッドの使い捨てスクリプトで作れば足りる（クレーム台帳のような常設ツールは作らない、という裁定）。

**書物層（`deliverables/book/`）には専用ゲート `tools/book_style_check.py` がある**（カタログ16・判断(ap)。`restore.py` も `source_sync_check.py` も書物層は見ない）。ただしゲートが覆うのは一部で、**残りの検品は毎回こちらがスクラッチのスクリプトで行う**（常設ツールは作らない）。新章・新幕間は `SHEET_REGISTRY`／`PORTRAIT_REGISTRY` への登録が要り、**登録漏れは落ちずに検査対象外になるだけ**なので、束の受け取りごとに登録の網羅を確認する。観点は5つ：①**データ層の照合**＝**無改変転写の対象は「できること」＝シートの `jp` と「例」＝`exponents` の二欄だけ**（`prototypes/catalog_<act>.json` を照合先に book_style_check が機械照合。差分が出てよいのは第8条の語彙置換〔レジスター→スタイル〕だけで、読点の増減も逸脱＝実際に2件出た）。**「場面」＝`scene`・落とし穴＝`l1`・「前の段から」＝`delta` は内容を保存した書き直し可**（レジスター変換規則1、カタログ16補遺で実態〔場面39/80・L1 0/80・delta 1/80〕と裁定に整合させた）── ここは逐語照合でなく、内容が保存されているかを精読で見る②**引用の照合**＝幕間のブロック引用・インライン断片が `working_translations_1224.json` と一致（縮約は第7条の `……` 表示つきのみ）。**ゲートが見るのは素材No.の全使用と出典包含までで、正準訳との逐語一致は見ない**（カタログ16で幕間A2のNo.409に無表示縮約を検出）③**散文中のレベル帯**＝見取り図・議論の帯言及がシートのモード別レベルと一致（機械ゲートが原理的に見ない層。カタログ13で「口頭A2〜C1」＝実際はB2+止まりを検出）④**参照の位置中立**＝「〜で見た」等の読了前提の言い回しは**自章内のみ**（巻頭・幕間も例外にしない ── 章の外は文書主語の位置中立形「〜の章にも現れる」「巻頭に掲げた」。旧例外「書物順先行章と巻頭」は判断(at)で撤廃、既知パターン5種は book_style_check が機械検出）、命名済み語彙は命名章より前では予告に留める（判断(an)(at)）⑤**No.の出自**＝章の段として挙げたNo.が篩DROPや留置でないか（実際に幕間で質の証言No.548を段のように提示していた）。段の集合はシートの行集合と完全一致するのが正（過不足・重複）。**順序は機械照合しない** ── 章は `##` で「書面の梯子／口頭の梯子」等の下位区分に割れてよく、その中でシート行順と違う並びになるのは正当（`##` の区切りを見てから判定すること。見ずに照合すると誤検出になる）。

**ドキュメントの散文と実データの食い違いは必ず指摘する**（レベル帯・件数など）。ただし直す向きは正準データで確かめること — 過去に指摘は正しく向きを誤った実績がある。パッチ運用と競合するので、パッチ対象外の行を勝手に書き換えず申し送りに回す。**空文字・未記入のフィールドも同じ扱いで拾う** ── 機械ゲートは meta の中身を見ないので、`"固有糸の規則": ""` のような書きかけが全緑のまま残った実績がある（カタログ11）。中身が設計判断なら埋めずに申し送りへ回す。

## WSL運用（毎回効いてくる）

ファイルはWindows側で作成され、このディレクトリにコピーされる。そのため：

- **コピー漏れを疑うこと。** ドキュメントが更新されたのに参照先の範型ファイルが来ていない、ということが実際に起きた。ドキュメントの記述と実ファイルを突き合わせてからコミットする
- **調査・検証のPythonは複数行の `python3 -c` で書かない**（この環境で固まる）。スクラッチパッドに `.py` を書いて `python3 file.py` で実行する
- **長い1行の出力を多段パイプに通さない**（`restore.py` の1,500字を超える1行を `tail | rev | cut | rev` に流して固まった実績。python自体は数秒で終わっているのにシェルが返らない）。出力はファイルへ落として `tail -c` / `sed -n` で読む

## git

リモートは `gh` のトークンを使うHTTPS（この環境にSSH鍵がないため）。`git push` は追加設定なしで通る。