# 検品報告 ── カタログ25 パッチ1補遺2（第3報）

**対象コミット**：`9a6aeab`（基底 `72c7b6e`）
**ゲート**：`restore.py` / `analysis/partition.py` / `source_sync_check.py` / `exponent_level_check.py` / `book_style_check.py` すべてgreen。段見出し17・議論5節・出典18件・見出しの級18件を再照合して不変。
**再測定**：全角換算 **18,432字**（記述「約18,400字」と一致）／落とし穴欄 実測 **197**（記述と一致）／「報告の側の最上級」0件・半角スペース＋級0件。

**第2報3件は全件閉じました。** ③の裁定（「終わりの段を主張せず、帯で共有を言う」）は、創作の文章の章が自分で使っている「C帯」の語法と揃っていて、C1（327）とC2（351）の両方を覆います。書物側はこれで整合しています。

新規は**1件**。散文の中身ではなく、**留保の宛先**です。

---

## 1. §7の留保が指す帳簿の場所が、実際の丸めの在処と三箇所ずれている

`引き継ぎ書:497`（§7）

> **留保（カタログ25補遺2）**：…帳簿の追随（**reports_essays／creative_writing の essence、ladder_templates の背骨記述**）は導出ビルド着手時にまとめて。

この留保は「あとでまとめて直す」ための住所録なので、宛先が違うとそのまま拾えません。grepで実際の在処を洗うと ──

**丸めを持っている（追随が要る）**

| 場所 | 文面 |
|---|---|
| `prototypes/catalog_p2_reports_essays.json` の `essence` | 「書面背骨の外枠（**接続に始まり読者適合に終わる**）はCreative writingと共有される」 |
| 同 **DISCUSSION 1** | 「**接続に始まり読者適合に終わる**外枠は系に依存せず（Creative と共有）、その中身…が系ごとに差し替わる」 |
| `data/ladder_templates.json` の背骨定義 | 「外枠（口頭＝受け手基準への漸近／書面＝**接続に始まり読者適合に終わる**）は系に依存せず」 |
| `引き継ぎ書:141`＝**判断(ac)(iii) の本文** | 「書面背骨の二層化 ── **接続に始まり読者適合に終わる**外枠は系非依存（Creativeと共有）」 |

**丸めを持っていない（追随は不要）**

| 場所 | 文面 |
|---|---|
| `prototypes/catalog_p2_creative_writing.json` の `essence` | 「梯子は組み立ての複雑化（接続→結束の明示→ジャンル慣習→**読者適合→効果**）に宿り」── 読者適合の**上に効果を置いていて、もともと正しい**。`ladder_templates` の段名列（「読者適合 style appropriate to the reader→効果 engaging / impact」）も同じく正しい |

ずれは三つです ── ①**`creative_writing` の essence は直す必要がない**（住所録に載っているが、そこは丸めていない）②**`reports_essays` の DISCUSSION 1 が漏れている**（essence と同じ文が二箇所ある）③**判断(ac)(iii) の本文が漏れている**（同じ補遺の §2(be) 側は「essence・**判断(ac)** の『読者適合に終わる』」と正しく名指していて、§7 とで割れています）。

宛先を次へ差し替えてください ──

> 帳簿の追随（`catalog_p2_reports_essays.json` の essence と DISCUSSION 1／`ladder_templates.json` の背骨定義／判断(ac)(iii) の本文）は導出ビルド着手時にまとめて。

なお §2(be) 側が「範型時点の分析の言い方として保全し」と書いているので、**判断(ac)(iii) の本文は履歴として保全する**という読みもありえます。その場合は §7 の留保に「判断(ac)の本文は保全、追随するのは帳簿3箇所」と明示してください ── いまは §2 と §7 のどちらが正なのかが読めません。

---

## 参考 ── 現時点の数値（すべて実測と一致）

- 全角換算 18,432字（README・§2(be)「約18,400字」）
- 落とし穴欄 197（§2(be)・§7）
- `book_style_check.py` 対象34ファイル・章のデータ層照合26
- `exponent_level_check.py` 773文、reports_essays の行はフラグ0
