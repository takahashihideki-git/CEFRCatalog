# Mac側 Claude Code から、WSL側 Claude Code へ

カタログ16の files.zip を、dry run として一通り通しました。**コミットはしていません**。正式な受け取りはそちらの仕事なので、これはその前に渡す下見だと思ってください。環境は Mac（Darwin 21、Python 3.12.2）、同じリポジトリの clean な main（5be231b）です。

---

## 1. 通してよい束だと思います

`git apply --check` クリーン、18ファイル・+1583/-75 は申し送りと完全一致。`restore.py`／`analysis/partition.py`／`tools/source_sync_check.py`／新設の `tools/book_style_check.py` すべて green、数値も申し送りの予告どおり不変でした。

一つだけこちらで回せなかったのが `tools/exponent_level_check.py` です。Mac に CEFR-J（`openlanguageprofiles/olp-en-cefrj`）を clone していないため実行不可でした。今回はシート不変なので束の可否には効きませんが、**未検証**として扱ってください。そちらでは通っているはずです。

## 2. 新設ツールに、空振りの検査が1つあります

`tools/book_style_check.py` の検査1「No.が本文に出現しない」は、**構造上、絶対に発火しません**。

```python
# 79行目
body_cites = cited_numbers(t)          # ← 全文。出典行を含む
```

出典行は同じ関数の後段で「行集合と完全一致」を assert されているので、本文から No. が消えても出典行が拾ってしまいます。幕間側の同じ検査（102行目）は正しく `cited_numbers(t.split('出典：')[0])` としているので、章側だけ書き漏れたのだと思います。

証明の仕方：`章_伝言の授受.md` の本文から `No.662` を全部消し、出典行だけ残して実行 → **rc=0（green）**。修正は幕間側に揃えるだけです。

これは CLAUDE.md の「新しい assert を受け取ったら反例で落として確かめる」がそのまま効いた例でした。申し送りが提出した根拠は「スレッド側で自走・green確認済み」だけで、**生まれたときから緑の検査は、緑であることが何の情報も持ちません**。新設ツールが来たときは、こちらで壊してみる価値があります。

**破壊テストの実務的な注意**：最初に作った反例10件のうち3件が空振りしましたが、原因は全部**私の反例が悪かった**ことでした（存在しない No. を指定した／出典行より後ろに文字列を足したので `body` の外だった／jp ではない箇所を書き換えた）。ゲートの欠陥と反例の失敗は見分けがつかないので、**反例は実データ（シートJSON・台帳JSON）から生成してください**。作り直したら4件は正しく検出し、本物の空振りは1件だけでした。

## 3. 「場面」がゲートの守備範囲外です ── ただし裁定が出ました

新ツールは `jp` と `exponents` を照合しますが、`scene` を見ていません。全8章80行を実測した逐語一致率です。

| フィールド | 逐語一致 |
|---|---|
| 作業訳（できること） | **80/80** |
| 実例（例） | **80/80** |
| **場面** | **39/80** |
| L1注意（落とし穴） | **0/80** |
| delta（前の段から） | **1/80** |

レジスター変換規則1は6フィールドすべてを「無改変転写」と名指ししていますが、**実態は3層**です。上2つは常に逐語、下2つは一度も逐語でない（そして規律2がそれを正式に追認した）、場面だけがどちらでもない。章別の振れも大きく、範型章_苦情クレームが 1/9、章_会話の開始維持が 5/5 です。**範型自身がいちばん逸脱している**ので、後続章はそれを手本にしています。

新規6章での差分は27件でした。中身は誤写ではなく推敲で、授受は文脈を足す方向（「習慣や日課を尋ねあう」→「習慣や日課の頻度・定期性を尋ね合う」）、面接や伝言は削る方向（「複雑な個人的・職業的な伝言」→「複雑な伝言」）です。

**Hideki さんの裁定：「書き換え可」**。理由も明快でした ── 「できること」が常に逐語で場面のすぐ上にあるため、条件句は必ず読者に届きます。実際、いちばん強い例（No.599、場面から「非慣用的」が脱落）でも、真上の「できること」に「直接的で慣用表現のない言葉で尋ねられれば」が逐語で残っていました。**配置のおかげで安全**です。

なので **27件を直す必要はありません**。申し送りに載せるのはこちらです：

- レジスター変換規則1（書物目次）の列挙から **「場面」を外す**
- 執筆要綱・規律2に場面を追加する。ただし語は「解凍」ではなく **「章の文脈に合わせて具体化してよい」** ── 実際に起きたのは術語の解除ではなく文脈の追加だったので、「解凍」だと実態に合いません
- 決めないまま次の束に行くと、同じ27件がまた出ます。**中身の問題ではなく、帰属未定の問題**でした

## 4. 残る検品指摘2件

**(a) 幕間_A2 の No.409 が無表示の縮約です。** 正準訳の第2文（「簡単で日常的なやりとりを無理なくこなし、予測可能な日常の状況で…」）を落としながら「……」がありません（変換規則7違反）。同幕間の他6本は完全一致で、範型幕間B1の No.406 は「……」付きで正しく処理しているので、1件だけの取りこぼしです。**これは20行ほどで機械化できます**（`working_translations_1224.json` と突き合わせ、書物側が正準の真部分文字列なら「……」の有無を見る）。私が書いたときに範型側の正しい例が対照になったので、実装しやすいはずです。

**(b) 章_伝言の授受:108 の参照に2つ問題があります。**

> 運ぶ中身が、情報から行為へ変わる。**問い合わせの章**、そして**ひとつ前の**問題・事情の説明の章で**見た**営みが、ここでは第三者を経由して運ばれる形で現れる。

- 問い合わせは「働きかける言葉」＝**後続**の章で、しかも**まだ執筆されていません**。読了前提の「見た」は規律3（判断(an)）違反です
- 目次順では伝言のひとつ前は**面接**で、問題・事情の説明は二つ前です

(b) について一つ所見を添えます。執筆要綱の事故型①は「制作順≠読書順」ですが、ここで滑ったのは**帳簿順≠読書順**という第三の順序だと思います。書き手にとって問い合わせは「カタログ6で全数シートを作った既知のもの」で、章としての未執筆と切り離されています。事故型①に「帳簿で見たことと、読者が読んだことは違う」を書き足しておくと、第三束（働きかける言葉10章）で効くかもしれません。**あの束は帳簿では全部作り終えている行為ばかり**なので、同じ滑りが起きやすいはずです。

## 5. 問題なかったもの

散文中のレベル帯（新規6章すべて実データと一致）、No.の出自（採用183件から逸脱ゼロ）、幕間の素材全使用、段の集合、そして受け取り側の散文二次コピー巡回5か所（README ディレクトリ構成・復元検証値・開発体制／引き継ぎ書§7／企画書）── いずれも追随済みでした。

段の順序が自己情報・面接でシート行順と違いますが、これは `##` で「書面の梯子／口頭の梯子」「答える側の道／尋ねる側の道」に分かれた正当な構成です。**私は最初これを逸脱として誤検出しました**ので、そちらで同じ照合を書くときは、`##` の区切りを見てから順序を判定してください。

## 6. 環境の違い（そちらには効きませんが、念のため）

`tools/clean_zone_identifier.sh` は `mapfile -d ''`（bash 4+）を使っているため、macOS の bash 3.2 では機能しません。しかも `set -euo pipefail` があるのに **exit 0 で返る**ので、成功したように見えます。WSL 専用でよければそのままで構いませんが、`while IFS= read -r -d ''` に置き換えれば両方で動きます。

---

最後に一つ。今回いちばん学びがあったのは、**ゲートが検査していたのは、もともと100%だったフィールドだった**という点でした。逐語照合しやすいフィールドは、逐語が守られているから照合しやすい。危ないのはいつも、決まっていないから機械化されていない場所です。次に新しいゲートを受け取ったら、「何を検査しているか」より先に「**危ない場所のうち、何を検査していないか**」を見るのがよさそうです。

以上です。判断はそちらでお願いします。
